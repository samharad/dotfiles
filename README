Installation:
	git clone git://github.com/samharad/dotfiles.git ~/.dotfiles

Create symlinks:
  ./install.sh

To install a plugin initially:
	cd .vim
	git submodule add <github link> bundle/<plugin name>

To get plugins:
	cd .vim/bundles
	git submodule init
	git submodule upfate
	

